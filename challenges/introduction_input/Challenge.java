
public class Challenge {

    public static void main(String[] args) {
        String user = args[0];
        int age = Integer.parseInt(args[1]);
      
        // This program has two input variables
        // Some text which we have named `user`
        // The user's age which we have name `age`
        // You will never have to chage the code about this line
        // Write your code below.

        System.out.println(user);
        System.out.println(age);
    }
}
